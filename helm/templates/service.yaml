apiVersion: v1
kind: Service
metadata:
  name: {{ include "golang-server-boilerplate.fullname" . }}
  labels:
    {{- include "golang-server-boilerplate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "golang-server-boilerplate.selectorLabels" . | nindent 4 }}
